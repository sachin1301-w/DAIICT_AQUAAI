# Python
__pycache__/
*.pyc
.venv/
venv/

# backend runtime data
backend/rec_fraud.db
backend/models/tx_isolation_forest_model.pkl
backend/models/tx_scaler.pkl
backend/models/tx_feature_names.json

# node / vite
node_modules/
frontend/dist/
contracts/cache/
contracts/artifacts/

# ml_training generated outputs (backend/models/ has the shipped copies
# the app actually loads -- these ml_training/-level ones are just the
# training script's raw output directory, regenerable via train_*.py)
ml_training/*.pkl
ml_training/*.json
ml_training/*.csv
ml_training/*.pt

# env
.env
